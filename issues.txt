1. turn off hover when no completed todo left

2. fix the completed-container margin-top 

3. adding features: 
3.1 edit, save & cancel

4. responsive design for mobile 
  4.1: keep the due date below the todo description

5. solving checkbox changed state  
> mark checked on complete todo listener
  > set the flag

> mark unchecked
  > unset the flag 

> check checkbox flag on renderTodo 
  > marked, set the DOM accordingly
  > Unmarked, set the DOM accordingly

--------------------------------------------------------------

algorithm for editing and saving a todo: [this will be written on todo.js button event listeners]

> on edit-todo.js create a function editedTodo(todo, date)
  > call currentTodoItem(todo, date)
  > get the edited todo & date with .value
    > check for null values 
      > if null 
        > assign currentTodo variables to editedTodo variables
      > else 
        > return the editedTodo variables
      
> event listeners: editButtonElement
  > show input value from index click handeler
  > call editButtonPressed()
    > Handel UI
    > call currentTodoItem() import from todo-edit.js
      > store in currentTodoObject 

> event listeners: saveButtonElement
  > call editedTodo()
  > set updated todo, date to push to todoList
  > hide the editing input and date fields
  > call saveButtonPressed()
  > call renderTodo()

> event listeners: cancelButtonElement
  > clear edit fields
  > call cancelButonPresses()
  > hide editing todo, date fields [must be on HTML] 
  > call renderTodo()

-------------------------------------------------------

before saving a edited todo, 
  check empty todo, 
  check the edited date, 
    then active the save button  


